/* 
  All the exercises have some requirements: read them and translate them to test cases for each one. 
  Often, some example input and expected output is provided: remove those from the exercise and use
  them in your tests.

  This file is for exercise1 only. Test file for the others exercises have to be created.
*/

const parseWeather = require('./01_parseWeather');

describe('parseWeather', () => {
  test('write a test!', () => {
    const testInput = {};
    const expectedResult = {};

    const result = parseWeather(testInput);

    expect(result).toEqual(expectedResult);
  })
});